# MCP Config Examples

This directory holds safe, token-free examples for MCP client configuration.

- `factory.mcp.json.template` shows the project-local Factory Droid HTTP MCP
  shape. Replace `YOUR_TOKEN_HERE` before using it as `factory.mcp.json`.
